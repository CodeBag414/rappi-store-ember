import { addressList } from 'rappi/helpers/address-list';
import { module, test } from 'qunit';

module('Unit | Helper | address list');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = addressList([42]);
  assert.ok(result);
});
