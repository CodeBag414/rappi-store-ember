import { googleMap } from 'rappi/helpers/google-map';
import { module, test } from 'qunit';

module('Unit | Helper | google map');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = googleMap([42]);
  assert.ok(result);
});
