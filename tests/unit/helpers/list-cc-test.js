import { listCc } from 'rappi/helpers/list-cc';
import { module, test } from 'qunit';

module('Unit | Helper | list cc');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = listCc([42]);
  assert.ok(result);
});
