import { expressCategoryName } from 'rappi/helpers/express-category-name';
import { module, test } from 'qunit';

module('Unit | Helper | express category name');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = expressCategoryName([42]);
  assert.ok(result);
});
