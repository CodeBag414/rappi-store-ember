import { headerAddressIcon } from 'rappi/helpers/header-address-icon';
import { module, test } from 'qunit';

module('Unit | Helper | header address icon');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = headerAddressIcon([42]);
  assert.ok(result);
});
