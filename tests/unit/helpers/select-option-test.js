import { selectOption } from 'rappi/helpers/select-option';
import { module, test } from 'qunit';

module('Unit | Helper | select option');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = selectOption([42]);
  assert.ok(result);
});
